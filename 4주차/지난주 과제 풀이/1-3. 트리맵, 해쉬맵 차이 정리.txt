트리맵은 고정적으로 O(log_2 n).
해쉬맵은 일정 데이터 개수 이하에선 O(1)로 동작하지만,
데이터 개수가 많아지면 O(n)으로 동작.

따라서 시간적 측면에선 데이터의 개수가 적당할 때는 해쉬맵,
매우 많을 땐 트리맵을 쓰는것이 보편적으로 이득임.

공간적 측면? 공간복잡도는 당연히 O(n)으로 같다.
하지만? 실제로는 해쉬맵이 더 많은 공간을 차지함.

지난주에 해쉬맵에서 array로 크기를 고정한다고 얘기했지만 사실 안그럼.